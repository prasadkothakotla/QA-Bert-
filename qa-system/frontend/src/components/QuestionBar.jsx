import './QuestionBar.css'
export default function QuestionBar({ question, onQuestionChange, onAsk, asking, disabled, selectedCount, totalCount, onSuggestion }) {
  function handleKeyDown(e) { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); onAsk() } }
  const scope = totalCount === 0 ? 'Add a document to begin' : selectedCount ? `Searching ${selectedCount} selected document${selectedCount === 1 ? '' : 's'}` : `Searching all ${totalCount} document${totalCount === 1 ? '' : 's'}`
  return <section className="question-bar"><div className="question-bar-heading"><span className="rail-eyebrow question-eyebrow">{scope}</span><span className="enter-hint">Press <kbd>Enter</kbd> to ask</span></div><div className="question-input-row"><span className="search-icon">⌕</span><textarea className="question-input" placeholder="Ask anything about your documents..." value={question} onChange={(e) => onQuestionChange(e.target.value)} onKeyDown={handleKeyDown} disabled={disabled} rows={1} /><button className="ask-button" onClick={onAsk} disabled={disabled || asking || !question.trim()}><span>{asking ? 'Searching...' : 'Ask question'}</span><b>→</b></button></div>{!question && totalCount > 0 && <div className="suggestions"><span>Try asking</span>{['Summarize the main ideas', 'What are the key takeaways?', 'Explain this simply'].map((item) => <button key={item} onClick={() => onSuggestion(item)}>{item}</button>)}</div>}</section>
}
